extends ./_template/_email_layout-rtl.pug

block meta 
    title ML0124(rtl)

block header
    +email-header-rtl({title: "Product disabled due to expiration of pricing"})

block content
    tr
        td(style=`color:#333333;${fontStyle_1}`)
            b(style=`color:#000000;${fontStyle_6B}`) Dear Customer,
            br
            br
            | 
            | You recevied this email, because your email in list monitor system email. List SKU is disabled below, plz verify if hace any misstake
    tr
        td(style='padding-top:30px;')
            table(cellpadding='0' cellspacing='0' width='100%' border='0' style='width:100%;border-top:1px solid #000000;')
                tr
                    td(align='center' style=`${thType1}`) SKU
                tr
                    td(style=`word-break:break-all;${tdType3}`) 1 UK.F1447TD.ABWQEUK

block footer
    +email-footer()
    